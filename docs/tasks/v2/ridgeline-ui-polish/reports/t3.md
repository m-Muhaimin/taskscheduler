# T3 Report — Dashboard motion + skeleton loading (Wave B)

**Task:** T3, Wave B — docs/tasks/v2/ridgeline-ui-polish/brief-t3.md
**Date:** 2026-09-19
**Status:** complete (type-check green, web build green)
**Author:** Forge (frontend engineer)

## Files

### Created
| File | Purpose |
|---|---|
| `apps/web/hooks/use-demo-load.ts` | `"use client"`; `useDemoLoad(): boolean` — starts `false`, flips `true` after a 350 ms `setTimeout`, or **immediately** when `useReducedMotion()` is true (consumes T1 hook; no artificial delay for reduced-motion users). Timer cleared on unmount; effect re-runs if the motion preference flips mid-load. |
| `apps/web/components/dashboard/dashboard-overview.tsx` | `"use client"`; props `{ metrics, inboxItems, appointments, recovery }`. `!loaded` → `<OverviewSkeleton />`; `loaded` → the exact previous `app/dashboard/page.tsx` markup (greeting, `MetricGrid`, `AiInboxList compact`, `TodayTimeline`, `RevenueRecoveryBand`). |

### Edited
| File | Change |
|---|---|
| `apps/web/app/dashboard/page.tsx` | Thin server component — fetches the four fixtures and passes them as props to `<DashboardOverview … />`. All presentational-component imports and the page markup moved into `dashboard-overview.tsx`. |
| `apps/web/app/dashboard/settings/page.tsx` | Google Calendar card: while `googleLoading`, the status line is two `Skeleton` rows inside the card; status copy and `Connect`/`Disconnect`/`Disconnecting…` copy kept verbatim. Connect button while loading keeps the permanent "Connect" label (was "Loading…") and stays disabled — loading text replaced per brief. Account card: while `useSession().loading`, name/email become two `Skeleton` rows (kills the "Owner"/"Not signed in" fallback flash); static Session row text kept. |
| `apps/web/components/dashboard/inbox-row.tsx` | Row entrance: `metric-card-rise` → `animate-rise`, delay `index * 55` → `index * 60`. No conflict with `.inbox-row` hover (`row-actions` lives on a child) or with `.is-resolved` (`resolve-fade` — `.inbox-row.is-resolved` (0,2,0) beats `.animate-rise` (0,1,0), same as before). |
| `apps/web/components/dashboard/escalation-banner.tsx` | Leading indicator is now a `flex items-center gap-2` cluster: `<span className="pulse-dot" data-tone="danger" aria-hidden="true" />` **plus** the existing `AlertTriangle` icon (icon kept — indicator + icon complementary). |
| `apps/web/components/dashboard/sidebar.tsx` | Identity block while `useSession().loading`: `Skeleton` circle (`h-7 w-7 rounded-full shrink-0`) + two `Skeleton` lines; brand, nav, sign-out remain real. |
| `apps/web/components/dashboard/top-bar.tsx` | Avatar while `useSession().loading`: `Skeleton` circle (`h-9 w-9 rounded-full`); title/switch/theme/hamburger remain real. |
| `apps/web/app/globals.css` | **Append-only**, one new rule set at EOF under a `/* ---------- T3: danger pulse tone (append-only) ---------- */` banner: `.pulse-dot[data-tone="danger"] { background: var(--danger); }` + `.pulse-dot[data-tone="danger"]::after { background: var(--danger); }`. Existing 543 lines untouched (T1's `.skeleton` + motion-token append and T2's edits predate this task and were verified in place). |

### No-change callouts (verified, per brief)
- `lib/session.tsx` — consumed only; already exposes `loading` (line 23). Not modified.
- `components/dashboard/count-up.tsx` — **not modified**: lines 10–14 already `matchMedia("(prefers-reduced-motion: reduce)")` → `setValue(target)` jump-to-final; the reduced-motion check passes.
- `components/dashboard/metric-card.tsx` / `metric-grid.tsx` — not reworked (brief said don't; CountUp + `delayMs` stagger already present).
- `components/dashboard/ai-inbox-list.tsx` — already fills the index at the list map (`visible.map((item, i) => <InboxRow index={i} … />)`), which is the brief's "pass i down" option; no edit needed.
- `components/dashboard/dashboard-shell.tsx` — listed in the brief, but it renders no identity itself: `TopBar` and `SidebarNav` are the session consumers (they already call `useSession()` directly), so the session-gate skeletons live exactly where the identity renders. No shell edit required.

## Skeleton surface list
1. **Overview first paint** (`dashboard-overview.tsx`): greeting → two `Skeleton` lines (h-7 / h-4); metric grid → 4 card-chromed blocks (`border border-border rounded-[10px] bg-surface p-4` + `Skeleton h-[148px]` → ~180 px total) in the real `grid-cols-2 lg:grid-cols-5 gap-3 mb-7`; 7/5 columns → inbox card = 4 `Skeleton h-16` rows with the real `[&>*+*]:border-t` divider pattern, timeline card = 5 `Skeleton h-14` rows. Column headers + "View all →"/"Full week →" links render real (static chrome). Shown for ≤ ~350 ms per mount; `aria-hidden` built into `Skeleton`.
2. **Settings async cards** (`settings/page.tsx`): Calendar status line and Account identity render `Skeleton` rows while their fetches are in-flight; all permanent copy preserved.
3. **Session-gate identity** (`sidebar.tsx` + `top-bar.tsx`): while `/api/auth/me` resolves, sidebar shows circle + two lines and top-bar shows a circle; no fallback initials/name flash.

## Reduced-motion behavior
- Overview: `useDemoLoad` resolves immediately under `prefers-reduced-motion: reduce` — skeletons disappear on the first effect tick (≤ one frame), real content rendered; no artificial delay.
- Count-up: unchanged — already jumps straight to the final value.
- Escalation pulse: `pulse-ring` opacity animation on `::after` is already disabled by the existing global kill-switch (`.pulse-dot::after { display: none }`); the danger dot itself renders as a static dot for reduced-motion users.
- Skeleton shimmer: `.skeleton::after` excluded under reduced motion (T1) — plain tonal blocks.
- Known pre-existing quirk (not introduced here, out of scope to fix): the global reduced-motion kill-switch does not zero `animation-delay`, so delayed-`rise` elements (metric cards `i*70`, inbox rows `i*60`) hold their `opacity: 0` start state for their delay before appearing. Identical behavior already existed for metric cards before this task; the brief's reduced-motion checklist (overview / count-up / pulse) is unaffected.

## Build / verification
- `npx tsc --noEmit -p apps/web/tsconfig.json` (run inside `apps/web`, workspace-pinned TS 5.9.3; root hoists TS 7.0.2 which rejects `baseUrl` — same quirk T2 hit) → **0 errors**.
- `npm run build --workspace=apps/web` → **green**, 14/14 static pages. Only the two documented cosmetic swc-lockfile warnings (CLAUDE.md "Known Issues"); no failures on any file.

## Files owned vs. parallel wave
Concurrent T4 (marketing/) and T5 (auth/) edits are visible in `git status` (auth-aside/auth-shell, marketing hero/controls/faq/etc.) — none of those are mine; I touched only the dashboard files above plus the globals.css append. The build is green _including_ the current T4/T5 in-flight state, so no foreign failures to attribute.

## Deviations
1. `settings/page.tsx`: `Connect` button label is the permanent "Connect" while `googleLoading` (was "Loading…") — the brief says replace loading spinner/text with skeleton rows; the skeleton occupies the status line, and the button reads correct copy in the disabled state. `Loader2` decorative icon removed from the static "Session" row (it never spun; it read as a loading spinner next to non-async copy). No permanent copy lost.
2. `dashboard-overview.tsx` skeleton: metric grid shows **4** blocks (brief's explicit count; real grid has 5 cards) and the RevenueRecoveryBand renders only in the loaded branch (it is not part of the enumerated skeleton surface; rendering it during the skeleton would have a real animated component flashing above placeholders — its `animate-rise`/sparkline entrance now plays with the loaded reveal).
3. `dashboard-shell.tsx` not edited — see no-change callouts.

## Open items for controller/vision
- Visual check: overview skeleton→data swap (~350 ms), settings skeletons while `/api/auth/me` and `/api/auth/google/status` are in-flight, danger pulse dot on the escalation banner (dark + light).
- Functional: reduced-motion emulation on the overview (no flash, no 350 ms wait) and on the pulse; row stagger on `/dashboard/inbox`.
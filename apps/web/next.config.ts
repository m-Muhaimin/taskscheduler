import type { NextConfig } from "next";

// Minimal config: shadcn CLI framework detection requires this file to exist.
// Next's App Router defaults are sufficient for the dashboard UI.
const nextConfig: NextConfig = {};

export default nextConfig;